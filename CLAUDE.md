# coffee_tracker — Backend (Go)

## Status: Maintenance

## Rules
- Same Clean Arch layer rules as root CLAUDE.md apply.
- Maintenance mode: prefer minimal-diff fixes. Don't refactor opportunistically.
- Any non-trivial change: check if it's actually warranted before touching it.

## Watch For
- This is an older codebase — likely has some domain logic in handlers. Flag but don't
  refactor unless the task explicitly requires it.
